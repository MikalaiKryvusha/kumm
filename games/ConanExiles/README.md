# Conan Exiles Enhanced — досье игры в KUMM

> **Создано:** 2026-09-18 · **Родитель:** `STATUS.md` — блоки перенесены оттуда ДОСЛОВНО при мягком закрытии
> 18.09, чтобы сводка снова читалась за один присест · **Статус:** 🧊 игра заморожена до ~25.09.2026, дальше
> переезд на 2.2.x · **Вовне:** —
>
> Пак — `D:\work\ai_sandbox\ConanExiles` (приватный репозиторий), его собственные документы — `docs/` пака.
> Здесь — то, что сессиям KUMM нужно помнить про Конан между заходами. Порядок блоков: сначала ближайшее
> (переезд), потом живое состояние, потом справочник. Тексты ниже — слово в слово как стояли в STATUS.

### 🧊 2026-09-18 — КОНАН ЗАМОРОЖЕН НА НЕДЕЛЮ. Дальше переезд на 2.2.x

**Слово владельца 18.09:** «через неделю займёмся 2.2.x — будем ставить и собирать на ней сборку,
обновлять наши моды». То есть примерно **с 25.09.2026**. До этой даты по Конану работы НЕ брать и
не предлагать: всё, что снято с 2.1.1, после переезда подлежит перепроверке.

**Конспект разведки — `researches/conan-2.2.0-gotovnost/README.md`.** Коротко: патч вышел 15.09,
движок 5.6.1 → **5.8.2**, сеть на Iris, моды прежней варки игра не берёт совсем. Чужие моды переезд
НЕ задерживают — **38 из 45 пересобраны за двое суток** (бета висела с 20 августа, половина
Мастерской перевыложилась в час патча). Отставших семь, ни один не блокер; жалко только Immersive
Warriors Enhanced (235 МБ своих анимаций), а Doubled Storage Size удалён из Мастерской совсем и не
вернётся.

**Прибор, который ответит на этот же вопрос в день переезда за минуту:**
```
cd D:\work\ai_sandbox\ConanExiles
python _config/check-mod-updates.py          # готовность по живому составу сборки
```
Спрашивает Steam и Nexus, печатает «OK / ЖДЁМ / ПРОПАЛ / НЕ ПРОЧЁЛ», код 1 если кто-то не готов.

**Порядок дня переезда (по разведке, §«Ловушки»):**
1. **Копия папки игры целиком ДО обновления.** Оно не приезжает само, и отката нет.
2. Обновление игры.
3. `python _config/check-mod-updates.py` — свежий счёт готовности.
4. Моды Мастерской анонимным steamcmd **в ОТДЕЛЬНУЮ папку**: `-force_install_dir` на соседний
   каталог. ⚠️ **Не перезаписывать `mods/_workshop/<id>`** — оттуда деплой кормит живую сборку.
5. Наши четыре Lua-мода: перемерить `VTableLayout.ini` и `MemberVariableLayout.ini` под 5.8.2
   (они едут вместе с версией движка — оплачено багом 10, строка `Unknown_1`). Заодно забрать
   UE4SS из main: поверх нашей сборки 1127 лежит девять коммитов, среди них **«fix: Conan Exiles
   Enhanced intermittent crash» от 12.09**.
6. `KrinikLevelUncapped` — пересборка. **Тут переезд в плюс:** кит на `F:` собран под 2.2.0,
   расхождение `EXP-0109` исчезает само и фаза 1 эпика `plans/06` разблокируется.
7. Графика перетюнивается заново: Funcom сам признал пересвет Lumen в подземельях «до следующего
   патча» и непересобранные PSO.

⚠️ **Урок этой разведки, чтобы не повторять:** состав сборки считается по `enabled` в манифесте и
сверяется с живым `modlist.txt`. Я доложил владельцу блокером мод, который он сам выключил неделю
назад, и от одного этого рекомендация развернулась на противоположную — `EXP-0115`. Второй урок про
прибор, совравший «ждём» вместо «не прочёл», — `EXP-0116`.

### ✅ Вечер 2026-09-16 — закрыто и принято, подробности в хронике

Кривая опыта доведена до якоря 500 и сверена с HUD; баг 12 закрыт мод-паузой 0.6.0 с двумя
замедлениями (меню `0.03`, ESC `0.0001`). Оба заказа проверены в игре и приняты владельцем.
Дословный разбор перенесён в `PROJECT_HISTORY.md`, запись «2026-09-16 вечер». Там же — почему
первое лицо с луком НЕ наша вина (правило игры, а не дефект `zoom_min`; подозрение с
`ConanCameraControl` снято). Уроки сессии: `EXP-0112`, `EXP-0113`, `EXP-0114`.

Ниже — только то, что осталось живым.

**Сейв откачен на 21-й уровень по просьбе владельца.** ⚠️ **Имена копий в `_save-backup\` были
ПЕРЕИМЕНОВАНЫ по проверенному уровню** (`…LEVEL21-verified.db` и т.д.): игра крутит свою ротацию
`Game_0_backup_N.db`, и снятые по старому списку имена соврали на один шаг. Читать уровень из
копии, а не из имени: `select level from characters`.

🧊 **ОТКРЫТО, НО ЗАМОРОЖЕНО ДО 2.2.x: мерцают тени от листвы под пальмами** —
`researches/conan-shadow-flicker/README.md`. ⚠️ **До переезда не трогать.** В 2.2.0 движок 5.8.2, и
Funcom сам признал переделку теневого и световного конвейера (пересвет Lumen с туманом «до
следующего патча»), а игроки сообщают о полосатых тенях на карте и волосах. Любой вывод, снятый
сейчас на 2.1.1, после переезда сгорит. Всё ниже — состояние на 16.09, перечитать ПОСЛЕ обновления.
Слово владельца 16.09 ~22:30: «тени от их листьев мерцают»; после трёх заходов — «лучше не стало,
мерцает». **Причина известна** (Epic: ветровой WPO обнуляет страницы теневого кэша каждый кадр;
случай учебниковый именно для 4K с подвижным солнцем). **Три рычага уровня cvar отвергнуты глазом
владельца** — SMRT 16/16, размытие движущихся теней (ещё и «качество ужасное»), и
`Cache.DeformableMeshesInvalidate=0`. Конфиг игры возвращён к исходному из копии, сверен хешем
`1CEB366F090691F3` — в папке игры наших опытных строк НЕ осталось.
**Следующей сессии — два шага, оба описаны в разведке:** (1) развилка `r.Shadow.Virtual.Enable=0`
одним заходом: пропало мерцание — виноваты тени, осталось — виноват апскейл DLSS (рендер 2562×1441
в 4K), и тогда весь вечер крутили не ту подсистему; (2) настоящее лечение по Epic лежит **на уровне
ассетов** (флаг «Evaluate World Position Offset» для теней, `WPO Disable Distance`,
`Shadow Cache Invalidation Behavior = Rigid`) — это Dev Kit и мод поверх листвы, то есть работа
эпика `plans/06`, а не вечерний тюнинг. ⚠️ И знать заранее: числовой замер страниц в этой сборке
НЕДОСТУПЕН (`r.ShaderPrint` вырезан), а сравнивать замеры между перезапусками нельзя — камера и
солнце уезжают.

**Что осталось у пары модов открытым:** смерть и возрождение при открытом меню; очередь крафта на
паузе; переживёт ли опознание ESC удаление мода Simple Modlist (`SML_ModList` — наблюдаемый
заменитель родного `IngameMenu`, который через перехват не проходит).

### 🗡️ Конан — справочник приборов, долгов и оплаченных правил (снят 13.09, живой)

> ⚠️ Это больше НЕ «первым делом в новой сессии»: первым делом — переезд на 2.2.x, запись 18.09
> выше. Здесь лежит рабочий справочник, который переезд переживёт: приборы, горячие клавиши,
> открытые долги и правила, оплаченные граблями. Читать при работе по Конану, а не вместо неё.

**Порядок передачи 13.09 ~23:20:** (1) рычаг спринта для `KrinikOblivionLocomotion` — пункт 0в и план 08, раздел
«❌ Поправка спринта не работает»; (2) недопроверенное у паузы — сундуки, станции крафта, смерть (пункт 0д, идея 05);
(3) у `KrinikLevelUncapped` не сверены очки атрибутов и знаний на экране «Хар-ки» (пункт 0г).
⚠️ **Устаревшая часть этого абзаца поправлена 18.09:** сохранение НЕ «с Проворством 2» — 16.09 сейв откачен на
21-й уровень (см. предупреждение про имена копий выше). Вход в мир агентом — `enter-world-focused.ps1`.
🧊 И всё это ждёт переезда на 2.2.x: до него работы по Конану не брать.

0в. 🟢 **Мод бега и прыжка `KrinikOblivionLocomotion` 0.2.1 — план `plans/08_conan_provorstvo_beg_pryzhok.md`.**
   Закон владельца: +3 см/с за очко Проворства к бегу, спринту и взлёту (было +1 → +5 → +3). Бег и взлёт по закону
   проверены в игре. **Следующий шаг — рычаг спринта:** спринт растёт пропорционально бегу, `PlayerSprintSpeedScale`
   игра не применяет (23:12, числа в плане 08). Открыто: касается ли общий множитель разгона NPC; пускает ли кнопка
   «+» Проворство выше 100 (слово владельца: не пустит — снимаем кап); мод камеры: `apply_zoom_range` в строке 990
   молча не работает (`EXP-0107`), не чинилось. Механизмы идеи 03 по шагам — `PROJECT_HISTORY.md`, запись «13.09.2026 вечер».


   **Зонд** `ConanExiles/_unpacked/ConanAttributeProbe` (раскатан): **Alt+Numpad .** — атрибуты и
   движение; **Alt+Numpad +** — серия 40 точек → `python _config/speed-from-log.py`; **Alt+Numpad −** —
   сторож 4 мин (только изменения); **Alt+Numpad \*** — бег, разгон, прыжок и пороги падения x2 (хук
   прыжка и переприменение после смерти — сами); **Alt+Numpad 0** — Проворство +10 (игра приняла 22);
   **Alt+Numpad /** — вернуть всё. Перед раскаткой Lua —
   `python _config/lua-globals.py <файл>`. ⚠️ **LeftAlt в Конане — уворот:** каждое нажатие
   кувыркает героя, поэтому бег меряется только серией (`EXP-0102`); правый Alt = AltGr (Ctrl+Alt).
   Голый нумпад и Ctrl+нумпад заняты камерой. Разбор графа — `t3d_trace.py`, `t3d_members.py`.
   Игру запускать `Start-Process`, затем `_config/enter-world-focused.ps1` (сам выводит окно вперёд и жмёт
   «Продолжить»); копия сохранения — `D:\Games\Conan Exiles Mods\_save-backup\`.
   ⚠️ `deploy-config.py` НЕ запускать: на 13.09 все четыре конфига игры разошлись с паком, раскатка
   затрёт `Engine.ini` и `GameUserSettings.ini` — сперва разобрать расхождение.

0г–0д. ↩️ Закрыты работой 16.09, дословно — `PROJECT_HISTORY.md`. Сейчас: `ideas/04` якорь 500, пауза `0.6.0`.

0. ✅ **UE4SS работает, баг 10 закрыт 12.09 — не открывать.** Моды UE4SS умерли — сторож
   `_config/enter-world.ps1` сам называет лечение (строка `Unknown_1` в `VTableLayout.ini`); полная
   страховка — `PROJECT_HISTORY.md`, запись 13.09, и `bugs/10_DONE_*`.

0а. 🔒 **Обезличивание — кодом:** `node tools/scrub-identity.mjs` (в новом клоне один раз `--install-hook`),
   канон — `AGENT_GUIDE.md` → git hygiene; `kumm` публичный. 🟡 Долг: приватный пак Конана не чищен
   (14 файлов и история). Полный текст пункта — `PROJECT_HISTORY.md`, запись 13.09.

1. ⚠️ **Долг класса «чужое обновление молча сотрёт нашу правку»:** локализация Hosav
   (`ConanExiles/_config/deploy-localization.py --check`) и `VTableLayout.ini` UE4SS — признаки в их README.

2. 🔲 **Фаза 0 эпика Dev Kit — `plans/07_epic06_phase0_chtenie.md`** (приборы чтения в
   `ConanExiles/_config/devkit/`): `devkit-run.ps1`, `dt-export.py`, `pak-inspect.py`, страж среды.
   Всё доказанное лежит в `researches/conan-devkit/` (`smoke-dump-xp.py`, `ftext_extract.py`,
   `textscan.py`, `glossary.py`) — переносить, а не переписывать. Оконный запуск кита — только
   по договорённости с владельцем (экран удалённого стола, 30–60 мин шейдеров).

3. 🟡 **Дождь (план 04 п. 4)** — цель найдена поимённо
   (`PersistentLevel.BP_Dynamic_Weather_Siptah_Rain_C....NS_Precipitation`), зонд Numpad 1 пишет
   в журнал (на экране ничем себя не выдаёт). Правка ещё не делалась.

4. 🟢 **Ждёт глаза владельца:** множители опыта подняты (`PlayerXPRateMultiplier=12`) — и ДОЛГ:
   пак не ведёт `ServerSettings.ini`, правка живёт только в папке игры (план 04 п. 5).

5. 🔧 **`[NOT-TESTED]`:** переживает ли глушение чужой камеры (`EAA_Zoom_C`) смерть и возрождение.

**Приборы пака, о которых стоит знать:** `_config/bisect-zoom.py` (деление сборки пополам правкой
`modlist.txt`, вердикт числом) · `_config/who-overwrites-table.py` (кто переписал строку таблицы, по
журналу) · `_config/backup/mods-asset-catalog.txt` (каталог 44 паков, 128 555 путей; теперь
ЛУЧШЕ — `UnrealPak -List` из кита, см. `EXP-0087`) · `_config/enter-world.ps1`, `capture.ps1`,
`close-game.ps1`, `input.ps1 -Key 0x09` (Tab), `run-camera-test.ps1 -WheelNotches 40`.
~~Агент не может нажать ни одной клавиши мода~~ — **опровергнуто 12.09** (`EXP-0097`): сырой код
`input.ps1 -Key 0x6E` дошёл до `RegisterKeyBind` зонда. У мода камеры канал `agent-cmd.txt` остаётся.

**Смерть героя в тестовой партии — НЕ беда** (слово владельца 13.09): после смерти герой оживает
с полным здоровьем и сытостью. Голодающего героя перед опытом не спасать и владельца об этом не
спрашивать. Но помнить: голод, жажда и низкое здоровье сами могут менять скорость и прыжок
(гипотеза владельца 13.09) — замер движения делать на СЫТОМ и здоровом герое, иначе цифры врут.

**Правила, оплаченные Конаном:** сырой поиск строк по пакам всегда ложноотрицателен (Oodle) —
теперь паки распаковывает `UnrealPak` кита · каталог говорит кто ЧТО ВЕЗЁТ, не кто ПОБЕДИЛ
(`EXP-0077`) · зелёный опыт обязан уметь краснеть (`EXP-0074`) · «хук встал» ≠ «хук работает»
(`EXP-0071`) · снимай и смотри глазами (`EXP-0081`), зрение исчерпаемо (`EXP-0084`).

### 🗡️ Третья игра — Conan Exiles Enhanced 2.1.1

Пак: **`D:\work\ai_sandbox\ConanExiles`** · приватный GH `MikalaiKryvusha/conan-exiles-modpack` ·
игра `D:\Games\Conan Exiles` · библиотека `D:\Games\Conan Exiles Mods\mods` (вне git).
UE **5.6.1**, билд `++exiles+release-CL-373655`, установка не из Steam, BattlEye выключен.

**Не пересказываю — ДЕСЯТЬ документов в паке, `docs/`:** `01-графика-и-DLSS.md` (шкалы настроек,
снятые чтением бинарника; лестница приоритетов cvar-ов; **раздел 8 — пять проверок в игре**) ·
`02-моды-и-инструменты.md` (pak+utoc+ucas, `modlist.txt`, анонимный `steamcmd` appid 440900,
**Ratchet — script extender Конана на Lua**, Dev Kit) · `03-cvars-конана.md` + `dw-cvars/`
(**собственное пространство `dw.*`, 264 переменные слоя Dreamworld, нигде публично не описанные;
конспект ПОЛОН, все девять семей**) · `04-камера.md` (разведка со скептиками) ·
`05-каталог-модов-nexus.md` (все 40 модов Enhanced) · `06-интерфейс-модов.md` · `07-зум-камеры.md` ·
**`08-мастерская-steam.md` (каталог Steam: 5 735 модов, 1 150 Enhanced, топ-120 с описаниями)** ·
**`09-рекомендации-модов.md` (отбор под вкус владельца + таблица конфликтов)** ·
**`10-жизнь-мира.md` (как сделать симуляцию жизни дёшево — под идею 02)**.

**Приборы пака** (`_config/`, все на python): `read-applied-cvars.py` — игра сама печатает в
журнал, что и из какой секции применила · `check-conflicts.py` — сталкивается ли наш `Engine.ini`
с этим · `deploy-config.py` — раскатка конфигов по хешу с бэкапом · `verify-install.py` +
`test-verify-baseline.py` — целостность игры и проверка самой проверки · `check-digest.py` —
полнота конспекта · `scan-cvars.py` — сканер cvar-ов из бинарников.

**Что применено к игре:** 4K, кап 141, VSync off, DLSS с пресетом **M** (`r.NGX.DLSS.Preset=13`),
Reflex, дальность и листва через группы качества `sg.*=4`, объёмный туман выключен, консоль
починена, множитель опыта 5.0, **одиннадцать модов** (девять с Nexus + два из Мастерской 09.09).
**Графика, моды и камера проверены владельцем 08.09; два новых мода и зонд снаряда — НЕТ.**

➡️ **ОЧЕРЕДЬ РАБОТ — `plans/04_conan_ochered_zakazov.md`** (восемь пунктов тикетами, по слову
владельца «будем потиху в разных чатах чинить это»). Состояние на 10.09.2026 ~12:00:
**закрыты и приняты владельцем** — HUD Hosav (1), зум камеры (2), камера после смерти (3),
интерфейс +10%, инерция зума и щуп земли. **Пункт 5 (перки) решён и ждёт РЕШЕНИЯ владельца,
а не работы.** Красного не осталось. Предыдущая очередь — `plans/03_conan_next_session.md`.

🔑 **РАМКА, СНЯТАЯ СО СЛОВА ВЛАДЕЛЬЦА 10.09:** он ещё НЕ НАЧАЛ настоящую партию — играет в
тестовом режиме, «обнулить не страшно», и начнёт долгую партию, «когда моды все более-менее
сделаем». Пока это так, **опыты можно ставить смело**: выключать моды, ронять прогресс, менять
систему прокачки. Копии сохранений всё равно снимаются перед каждым таким опытом
(`D:\Games\Conan Exiles Mods\_save-backup\`). **Как только он объявит начало партии — рамка
закрывается, и цена любого опыта над сохранением становится настоящей.**

### 🧰 Dev Kit Конана — кит патча 2.2.0 (UE 5.8.2), игра у нас 2.1.1

**`F:\CEDevKit\CEUE5Devkit`.** Кит собран под патч 2.2.0: моды из него игра 2.1.1 не примет (UE5 1018 против
1017, `EXP-0109`). **Патч 2.2.0 вышел 15.09.2026** — то есть расхождение снимется САМО в день переезда на
2.2.x (~25.09), и это главный выигрыш переезда: фаза 1 эпика `plans/06` разблокируется, а `KrinikLevelUncapped`
впервые соберётся китом, а не распаковкой. Донор для распаковки, если понадобится, — MrSerji's Level 1000
(Nexus 15), перевыложенный 17.09 уже под новый движок. Безоконный Python-командлет (~2 мин) — рабочий
прибор чтения таблиц и отражения классов. Разведка — `researches/conan-devkit/`, эпик — `plans/06`. Оконный
запуск — только по договорённости с владельцем; `zenserver` гасить `F:\CEDevKit\CEUE5Devkit\Engine\Binaries\Win64\zen.exe down`.
Прежний полный текст — `PROJECT_HISTORY.md`, запись «13.09.2026 вечер».

### Стрельба из лука — в дальнем ящике

- 🅿️ **Стрельба из лука «по-обливионски» — В ДАЛЬНЕМ ЯЩИКЕ по слову владельца 12.09 ~15:50.**
      Разведка закончена и оказалась неожиданной: **механика уже в игре, обе половины.** Баллистика —
      `UConanProjectileMovementComponent`, `ProjectileGravityScale`, `ProjectileGravityCurve`,
      `ProjectileSpeedCurve`, зависимость от натяжения. Втыкание — отдельный чертёж
      `BP_DummyProjectile` с `K2_AttachToComponent`, `MyBoneName`, `PenetrationDepth`, `IsLootable`,
      `SetLifeSpan`, а в родителе `BP_BaseProjectile` — `ShouldAttach`, `SpawnAttached`,
      `DummyProjectileAttachedToCharacterLifetime`, `DummyProjectileOnGroundLifetime`. То есть задача,
      скорее всего, **не «сделать», а «настроить»**. Прочитаны ИМЕНА полей, не значения. Всё с адресами
      ассетов, тремя дорогами доставки и рисками — `researches/conan-archery/README.md`; наблюдение в
      игре владелец взял на себя — `homeworks/02_conan_strely_nablyudenie.md`. **Не поднимать, пока он
      не скажет.**

