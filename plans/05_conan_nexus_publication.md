# План 05 — публикация мода камеры на Nexus

> **Создан:** 2026-09-10 ~17:30 +03:00, по слову владельца: «этот чат закрываем, в новом чате
> будем чинить ось обливиона и новые скриншоты снимать»
> **Родитель:** решение владельца выложить `ConanCameraControl` на Nexus (чат 10.09) ·
> `ConanExiles/_unpacked/ConanCameraControl/NEXUS_PAGE.md`
> **Статус:** 🟢 оба прежних блокера сняты 2026-09-10 вечером · 🟡 открыто три хвоста:
> стиль подписи ждёт выбора владельца, ванильный кадр не снят, посадка по умолчанию не назначена
> **Вовне:** публикацию делает ВЛАДЕЛЕЦ; агент готовит материалы

## Что закрыто вечером 10.09.2026

### ✅ Блокер 1 снят — но не так, как собирались

Собирались чинить удержание «оси вращения». Оказалось, **чинить было нечего: ось не могла дать
наклона в принципе**, а удержание сделало хуже.

- `TargetOffset` — не поворот. Заголовок Unreal дословно: *"Offset at start of spring, applied in
  world space"*; поле прибавляется к началу штанги в мировых координатах, направление взгляда о
  нём не знает. Проверено кадром при `target_offset_z = 200`: горизонт остался ровно посередине
  экрана (взгляд не наклонился), герой уехал за нижний край. То есть это был **дубль `offset_z`**.
- Удержание стоило **покадровой драки**: 12 300 возвратов за семь минут, 26 записей в секунду.
  Владелец увидел это сразу — «и дёргалась камера, когда ты её поднял». Тот же класс дрожи, что
  лечил баг 04, только теперь его создавало лечение.
- **Настоящий угол берётся у КАМЕРЫ, а не у штанги.** Разведка `probe_camera_angle`:

  ```
  штанга.bUsePawnControlRotation = true    поворотом штанги владеет контроллер (мышь)
  камера.bUsePawnControlRotation = false   А КАМЕРА СВОБОДНА
  камера: CameraComponent ...BasePlayerChar_C.CameraThirdPerson
  ```

  Записали −12°, через полторы секунды в поле те же −12° **без единой повторной записи**.

**Что стоит в моде теперь:** `camera_pitch` и `step_pitch`; клавиши **7 / 1** и **Ctrl+9 / Ctrl+3**
(шаг 1°), **Ctrl+5** возвращает штатный ракурс; наклон входит в записываемую посадку (`/` и `0`).
Ось вращения убрана целиком — и удержание, и шесть клавиш.

**Сторож наклона с выключателем драки.** Игра переписывает угол после возрождения (замер: у нового
персонажа `Pitch=4.0` вместо нашего), поэтому расхождение исправляется. Но при частоте выше
тридцати возвратов в минуту сторож **гасит себя сам** и говорит об этом вслух: дрожащая камера хуже
сброшенного наклона. Порог с запасом в обе стороны — редкий сброс даёт единицы, покадровая драка
дала бы больше тысячи.

### ✅ Блокер 2 снят — и вскрыл причину прошлого провала

**Агент не может нажать ни одной клавиши мода.** `Tab` до игры доходит (инвентарь открывается), а
`Numpad` и `Ctrl+Numpad` до UE4SS — нет: ни виртуальной клавишей, ни скан-кодом, ни при удержании
300 мс, при включённом NumLock и игре в фокусе. Значит **все прежние камерные опыты «с нумпада»
были холостым ходом**, а слово «проверил» — неправдой. Отсюда и три одинаковых кадра прошлого
захода: настройка не менялась, менялись только подписи.

Лечение — **канал без клавиш `agent-cmd.txt`**: мод раз в секунду читает файл и применяет
`ключ = значение`, плюс `probe = angle` и `probe = pitch:<град>`. Им снята вся серия.

**Снято 14 кадров в родных 3840×2160**, все машинно различны (самая похожая пара 2.65 %; одинаковые
дали бы 0.00 %). Приборы перенесены в пак — `ConanExiles/_config/nexus/`, там же README с порядком
работы.

**Правило прицела стало сторожем, а не заметкой.** Слово владельца: «прицел должен быть выше
головы». Прицел — центр экрана, значит герой обязан быть ниже:

```
герой ниже прицела, градусов = atan(offset_z / arm_length) − |camera_pitch|
```

Первая серия проваливала это на трёх дальних кадрах: на дистанции 950 подъём в 200 даёт всего 12°,
а наклон в 14° съедал его целиком. Владелец назвал два из трёх; третий нашёлся формулой. Теперь
`shoot-series.ps1` считает угол ДО съёмки и **отказывается снимать** кадр ниже порога.

## 🟢 Страница на Nexus заведена — состояние 11.09 10:40

**Черновик живёт здесь:** `nexusmods.com/games/conanexilesenhanced/mods/46/edit/general`,
статус **Unpublished**, автор `kotkrinik`, игра **Conan Exiles Enhanced**, категория
**Miscellaneous** — единственная, какую эта игра на Nexus предлагает, проверено списком.

**Сделано и проверено:**

- ✅ **Имя:** `Krinik Camera Behind - Third Person Camera Control`.
- ✅ **Короткое описание** — 268 символов из 350.
- ✅ **Полное описание** — оба языка, BBCode, 7168 символов. Проверено на свежей загрузке:
  **ровно одна копия, шаблон Nexus вычищен**. Шаг General помечен зелёной галкой.
  Текст лежит в паке: `_config/nexus/nexus-description.bbcode.txt`.
- ✅ **Архив собран и раскрыт для проверки:** `_unpacked/ConanCameraControl-1.0.0.zip`,
  60 КБ, четыре файла. Сборщик — `_config/nexus/build-archive.py`.

**Осталось — по шагам формы:**

1. 🔲 **Files** — отдать архив. Кнопка «Add file» на мышь не открыла диалог с первого раза;
   у Nexus тут ещё есть перетаскивание файла в зону.
2. 🔲 **Media** — логотип `nexus-logo/nexus-logo.jpg`, схема `nexus-camera-comparison.webp`,
   15 подписанных кадров из `nexus-screenshots/podpisannye-A/`.
3. 🔲 **Permissions** — необязательный шаг, но лучше заполнить осознанно.
4. 🔲 **Publish** — зелёная кнопка в шапке.

### Грабли этого захода, оплаченные временем

- **Редактор описания перехватывает Ctrl+A.** Три вставки подряд дописались в конец вместо
  замены, и на странице оказался шаблон Nexus плюс три копии текста. Лечится кнопкой `[ ]`
  в панели редактора: она переключает в исходный BBCode, где выделение работает.
- **Запись в textarea через DOM не сохраняется** — поле лишь зеркало визуального редактора,
  состояние приложения от неё не меняется. Save нажимается, но на сервер уходит старое.
- **`kumm eval` работает в СВОЕЙ вкладке**, не в той, что видна на экране: навигация из него
  видимую вкладку не трогает, а каждый вызов перезагружает страницу и теряет несохранённое.
- **Раскладка клавиатуры русская**: буквенные клавиши в поля вводят кириллицу. Всё, что надо
  набрать, идёт через буфер обмена.
- **Фокус забирает окно чата**, когда владелец печатает. Наведение Chrome и действие должны
  жить в ОДНОМ вызове, иначе клик уходит в редактор кода.

## 🟡 Что осталось — состояние на вечер 10.09

**Две буквы за владельцем.** Оба вопроса вкусовые, поэтому предъявлены листами, а не решены агентом:

1. **Бронза логотипа** — `nexus-logo/LIST-LOGOTIP.jpg`:
   A тёплая плакатная · B чеканка с узким горячим бликом · C старая с патиной.
   Полные 4096 всех трёх лежат рядом; в git они не идут — пересборка любого стоит одного
   прогона `logo.py`, а три почти одинаковых кадра по 5 МБ история носить не обязана.
2. **Разделение языков на подписи кадров** — `nexus-screenshots/LIST-PODPISI-vblizi.jpg`
   (вблизи, там и видна разница) и `LIST-PODPISI-v-kadre.jpg` (целиком в кадре):
   A русский золотом, английский белым · B оба заголовка белые · C только заголовки.
   После буквы — один прогон `caption.py --style <буква>`.

**Что ждёт владельца помимо букв:**

3. **Вычитка текстов.** 14 подписей — заголовок и поясняющая строка на двух языках — написаны
   агентом ПО САМИМ КАДРАМ (серия открыта и просмотрена до того, как написана первая строка).
   Живут в `_config/nexus/caption.py`, словарь `CAPTIONS`. Это голос владельца, ему и править.
   Туда же `NEXUS_PAGE.md`.
4. **Ванильный кадр не снят.** Требует выключить мод и перезапустить игру (`shoot-vanilla.ps1`
   делает это и возвращает мод даже при отказе — через `finally`). Ждёт слова владельца, чтобы
   не сбить его с выбранной точки.
5. **Посадка по умолчанию не назначена.** В `config.lua` сейчас `camera_pitch = 4.0` — это НЕ
   выбор, а след. Нужен его номер кадра из серии.
6. **Название утвердить.** На логотипе стоит `KRINIK CAMERA BEHIND` (по имени файла исходника,
   как он и просил) и ниже `THIRD PERSON CAMERA CONTROL`.
7. Собрать архив мода ровно в том виде, в каком его будут качать, и проверить установку с нуля
   по своей же инструкции.

### Два изъяна съёмки, которые видит глаз

- **Подсказка новичка** («Выживший — Сделайте и наденьте одежду») висит справа во ВСЕХ
  четырнадцати кадрах. Для витрины это шум, и убирается он только пересъёмкой с закрытой
  подсказкой — то есть заходом владельца в игру.
- **`11-shoulder-left`** поймал героя в позе с раскинутыми руками: кадр выбивается из ряда,
  где везде спокойная спина.

### Сделано вечером 10.09

- **Логотип.** `logo.py` пишет название на исходник владельца шрифтом **Crom**. Объём собран
  пятью проходами: падающая тень · экструзия шагом вниз-вправо · лицевой градиент металла ·
  скос по верхней и нижней кромке · тёмный контур. **У Crom нет кириллицы** — проверено
  отрисовкой: латиница и цифры выходят, русская строка пуста. Логотип говорит по-английски.
- **Подпись кадров.** Стиль старого захода снят числами и живёт теперь в одном месте —
  `sign_style.py`. Раскладка идёт по БАЗОВЫМ ЛИНИЯМ: замеренные 33 и 57 пикселей это шаг между
  базовыми линиями, а не пустота между строчными боксами, и прочитанные как пустота они
  разносили блок вдвое. Тень кладётся по габариту текста с размытием 0,6 % ширины — за слова
  выходит на волос, полосой не становится.
- **Схема сравнения двуязычна**, подписи парные: русская строка и под ней английская, помельче
  и светлее. Русский первым, как и на кадрах; порядок меняется одним словом.
- **Сырая съёмка перенесена в пак.** Кадры в `nexus-screenshots/` УЖЕ были подписаны прошлым
  заходом, и новая подпись легла поверх старой — двоение видно на первом же листе. Сырые
  3840×2160 нашлись в скретчпаде прошлой сессии и скопированы в пак; умолчание `OutDir` в
  `shoot-series.ps1` показывало на тот самый скретчпад и переписано на пак.

## Грабли, оплаченные этим заходом

- **`UE4SS.log` стирается при каждом запуске игры.** Улики снимать ДО перезапуска — иначе
  доказательство исчезает вместе с логом (так пропал журнал сессии, где появилась четвёрка в конфиге).
- **PowerShell 5.1 с `-Encoding utf8` пишет BOM.** Канал мода на нём молча споткнулся: файл на вид
  правильный, команда не исполняется. В моде теперь BOM снимается, в скриптах пишем
  `File.WriteAllText`.
- **Русская локаль пишет дробное через запятую** — и в CSV это лишняя колонка. Числа в манифест
  идут через `InvariantCulture`.
- **Игру НЕ ЗАКРЫВАТЬ** между кадрами: вся серия — один заход.
- **Окно игры должно быть в фокусе**, иначе `input.ps1` честно отказывается слать нажатия.

**Публикует владелец.** Агент к чужим площадкам от его имени не ходит.
