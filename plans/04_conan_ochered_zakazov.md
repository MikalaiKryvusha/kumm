# План 04 — очередь заказов владельца по Конану (вечер 09.09.2026)

> **Создан:** 2026-09-09 22:40 +03:00, по прямому слову владельца: «записал всё тикетами, что я
> тебе набрасывал? будем потиху в разных чатах чинить это»
> **Родитель:** вечерняя сессия 09.09 · `plans/03_conan_next_session.md` (предыдущая очередь)
> **Статус:** 🟢 готов к разбору по одному пункту за чат
> **Вовне:** решения по пунктам, помеченным «слово владельца»

## Как этим пользоваться

Каждый пункт самодостаточен: в нём есть **что просил владелец его словами**, **что уже известно**
(измерено, а не предположено) и **следующий шаг**. Новый чат берёт ОДИН пункт и закрывает его.
Пункты не связаны между собой — порядок можно менять.

⚠️ **Читать перед любым пунктом про камеру или дождь:** за вечер 09.09 агент трижды выдал
правдоподобное рассуждение за факт, и каждый раз это проверял владелец своими запусками игры.
Правило на эту очередь: **сперва измерение, потом правка.** Приборы для этого уже написаны
(`ConanExiles/_config/`: `enter-world.ps1`, `capture.ps1`, `input.ps1`, `frame-diff.py`,
`run-camera-test.ps1`, `close-game.ps1`) — агент запускает игру и снимает числа САМ, без владельца.

---

## ✅ 1. Увеличить ТОЛЬКО HUD от Hosav — СДЕЛАНО 10.09.2026, принято владельцем

**Слова владельца:** «умоляю тебя! Найди способ UI вернуть в тот размер, что был. И ТОЛЬКО ВОТ
ЭТОГО МОДА ВВЕРХУ HUD УВЕЛИЧИТЬ РАЗМЕР». Приёмка: «HUD ты увеличил. спасибо, молодец».

**Как решено.** Виджет найден поимённо: **`W_SmoothHP_C`** внутри контейнера `W_NewHealthUI_C` —
это и есть блок вверху экрана (здоровье, стамина, еда, вода, порча, температура, опыт, броня, вес,
компас, пинг; 133 свойства, 75 функций). Масштаб **1.25** через `RenderTransform`, точка роста —
левый верхний угол (слово владельца: «нужно, чтобы он рос внутрь вправо, а не обрезался слева краем
экрана»). Возвращается после каждого пересоздания виджетов: смерть, возрождение, выход в меню.
Настройка — `_unpacked/ConanModPackStatus/config.lua`, ключ `hud_scale`.

**Почему путь безопасен, а не эксперимент:** сам Hosav держит свой `W_EnemyBuffList` на
`RenderTransform.Scale = 0.600`. Мы делаем то же самое тем же полем, только числом больше. Виджет
не создаётся — создание роняло игру пять раз из пяти (`EXP-0060`, `EXP-0061`).

**Как нашли класс.** `UIMod_Hosav.pak` оказался ОБЁРТКОЙ: внутри три контейнера IoStore. Наш
офлайн-индекс их читает — 204 ассета за секунды, без запуска игры. Живые экземпляры и устройство
классов сняты зондом `ConanModPackStatus` (`probe_widget_prefix`) — только метаданные.

**Отдельно принято:** `ApplicationScale = 1.1` — весь интерфейс +10% по заказу владельца («и можно
интерфейс на 10% увеличить средствами всего UI»), с его же оговоркой, что это НЕ заменяет заказ по
HUD. Приёмка: «UI 110% — ОК, Спасибо».

---

## ✅ 2. Камера: рывки и предел — РЕШЕНО 2026-09-10 ~11:40, принято владельцем

**Виновник — `Enhanced Armory and Arsenal`**, найден делением сборки пополам за ДВА прогона.
Это не дефект мода, а его заявленная функция («Extended Camera Zoom, scroll out the view like
never before»). Мод остался в сборке ЦЕЛИКОМ: наш `ConanCameraControl` останавливает тик его
камерного компонента `EAA_Zoom_C` (ключ `mute_foreign_camera`), остальное работает.
Доказано парой прогонов: глушилка ВЫКЛ — цель 2100 при пределе 1000, штанга скачет 1753..1799;
глушилка ВКЛ — цель 1000.0, штанга неподвижна. Слово владельца: «да, сейчас баг ушёл».

**Полный разбор — `bugs/09_DONE_conan_zoom_runaway.md`.** Там же главный урок захода: первая
проверка была слепой (харнесс крутил колесо 12 щелчков, этого хватает ровно до предела), и поймал
это только контрольный прогон.

Заодно возвращены две настройки, сбитые вчерашней погоней не за тем слоем, обе приняты владельцем:
`zoom_interp_speed` 1.5 → 9.0 («инерция сработала») и `disable_collision_test` true → false
(«колизия камера сработала»). Плюс `zoom_min` 60 → 30 по заказу «ближе перед переходом в FPV» —
это ЖДЁТ ПРОВЕРКИ ВЛАДЕЛЬЦЕМ.

<details><summary>Прежний текст пункта (состояние на 00:30, до находки)</summary>

Главное, что изменилось за вечер: **это не наш мод.** Слово владельца — «пока модов из стима не
наставили, а был только наш рукописный мод вчера — всё хорошо было с зумом. Был лимит, не было
рывков». Измерение подтверждает: наш мод пишет ВСЁ, что должен, а симптом остаётся.

Что снято с подозрения ИЗМЕРЕНИЕМ и не надо переискивать: перехваты UE4SS по функциям блюпринта
(пять точек встали, ноль срабатываний) · запись в `NewTargetArmLength` (проходит и затирается) ·
`ThirdPersonCameraMaxArmLength` как предел (игра его не соблюдает) · мод
`Player Ignores Follower Collision` (выключен — не помогло).

**Следующий шаг — деление сборки пополам.** 35 оставшихся модов Мастерской, 5–6 запусков. Метод не
зависит ни от каких догадок, в отличие от всего, что пробовалось вечером.

</details>

---

## ✅ 3. Камера после смерти — СДЕЛАНО 10.09.2026, принято владельцем

**Слова владельца:** «после смерти и возрождения камера справа за плечом — плохо». Приёмка:
«после смерти камера — ОК, спасибо».

**Причина, снятая журналом, а не догадкой.** В момент смерти штанги персонажа исчезают, и остаётся
одна — у актёра `DeathCameraActor_C`. Мод перецеплялся на неё:

```
23:33:08  ПЕШКА ИГРОКА: DeathCameraActor_C
          найдено пружинных штанг: 1
          ВЫБРАН компонент: DeathCameraActor_C....SpringArm
```

Дальше главное: этот актёр живёт в уровне и ПОСЛЕ возрождения. Проверка мода звучала как «моя
штанга ещё цела?» — она цела, поэтому пересбор не запускался никогда, и настоящая камера персонажа
оставалась нетронутой.

**Лечение в двух местах:** `pick_arm` не берёт штангу камеры смерти вовсе; `keep_target_body`
отцепляется от неё, если уже взял. Урок общий: **мало спрашивать, ЖИВА ЛИ штанга — надо спрашивать,
ТА ЛИ ОНА.**

---

## 🟡 4. Дождь «появляется из воздуха» на отдалении

**Слова владельца:** «дождь виден на больших отдалениях, появляется из воздуха; нужно сильно
повысить высоту дождя», позже — «дождь всё ещё сломан».

**Что известно:**
- ❌ **Теория охвата съёмки ОПРОВЕРГНУТА.** `OrthoWidth` у `Weather_SceneCapture2d_Rain`
  переключался владельцем 12 раз между 4096 и 12288 (каждое подтверждено чтением обратно) —
  картинка не изменилась ни разу. Правка откачена, `rain_ortho_width = 0`.
- ✅ **Настоящая цель найдена:** `FLX/VFX/Particles/Weather/NS_EXL_Precipitation` — система
  частиц осадков. Механизм из документации Unreal: дождь спавнится в КОРОБКЕ у игрока, у эмиттера
  ФИКСИРОВАННЫЕ границы; дальность задают размер коробки и эти границы.
- Разбор целиком — `ConanExiles/docs/11-дождь.md`.

**Следующий шаг:** нажать **Numpad 1 ВО ВРЕМЯ ДОЖДЯ** — зонд теперь покажет компонент
`Precipitation` (раньше мешал потолок в 12 строк, который забивала песчаная буря из ролика).
Прочитать его параметры и найти те, что задают коробку.

---

## ✅ 5. Перки: виновник назван контролем и ЗАМЕНЁН (2026-09-10) — принято владельцем

**Слова владельца:** «какое-то пиздец с перками» + скриншот дерева Авторитета (10.09 прислан снова).

**⚠️ ПРЕЖНИЙ ВЫВОД СНЯТ.** Здесь стояло «строки нет ни в одном из 44 паков, контроль пройден —
значит из самой игры». Вывод получен НЕГОДНЫМ способом: сырым поиском строк по пакам, а содержимое
паков сжато Oodle — такой поиск не находит ничего НИКОГДА. Тот же промах, что и с «камеру не трогает
ни один мод».

**Что показал офлайн-каталог имён (128 379 путей по всем 44 пакам):** перковые таблицы везут **два**
мода, а не ноль.

| Мод | Что везёт |
|---|---|
| `MrS_LevelAndAttributeOverhaul` | `DT_MrS_PerksTable_Authority` — дерево ровно с тем именем, что на скриншоте, — и ещё шесть (Agility, Strength, Vitality, Corrupted, Expertise, Grit), плюс `DT_MrS_AttributeSystem`, `DT_MrS_FeatsPerLevel` |
| `EnhancedArmoryAndArsenal` | `DT_EAA_PerksTable`, `DT_EAA_AttributeSystem`, `ClassPerkTree2`, `BeastBinderPerkTree2`, `JackalPerkTree2`, `IronWardenPerkTree2`, `GoldTonguePerkTree2` |

⚠️ **И ЭТОТ ВЫВОД ТОЖЕ ОКАЗАЛСЯ НЕВЕРЕН** — оставлен здесь как след рассуждения, чтобы к нему не
возвращались. Стояло: «две сборки правил на одну систему атрибутов одновременно, и пустые слоты —
цена их наложения». Наложения нет: MrS не переписывает ни одной чужой строки, а плейсхолдеры —
его собственные. Правильный ответ ниже, и добыт он контролем, а не чтением каталога. Урок общий:
каталог имён говорит, кто ЧТО ВЕЗЁТ, и не говорит, кто ПОБЕДИЛ; второй вопрос решается журналом
игры и выключением.

**РЕШЕНО 2026-09-10 ~11:50. Виновник — `MrS_LevelAndAttributeOverhaul`, и это НЕ конфликт модов.**

Владелец спрашивал прямо: «может какая-то комбинация наших модов?» Ответ — нет.

**Доказательство — контрольный прогон, а не рассуждение.** Агент сам зашёл в игру, открыл
«Хар-ки» и снял экран в двух состояниях одного и того же персонажа:

| | с модом MrS | без него |
|---|---|---|
| дорожки перков | длинные ленты, узлы `?` | ванильные, по пять узлов, настоящие иконки |
| подписи | «Placeholder Perk — Just a Placeholder» | «Тяжелая рука», «Сложные связки», «Берсерк» |
| язык | **английский** | **русский** |

Снимки: `perki-S-modom-MrS.png` · `perki-BEZ-moda-MrS.png` (отданы владельцу в чат 10.09).

**Три улики, найденные ДО контроля и им подтверждённые:**

1. MrS — **единственный из 44 модов**, кто монтирует ассеты в ВАНИЛЬНЫЙ путь
   `Content/UI/Inventory/Attributes/` (`W_Attribute`, `W_AttributeInfo`). Все прочие, включая EAA
   с его `ClassPerkTree2`, держат перковые окна у себя в папке. Значит дерево рисует он.
2. Он везёт `IMG_MrS_Perk_Level_Unknown` — иконку-заглушку рядом с иконками уровней 005…100.
3. **Конфликта нет вовсе:** прибор `_config/who-overwrites-table.py` по журналу игры показал, что
   MrS не переписывает НИ ОДНОЙ строки чужих таблиц (`overrides 0 path(s)`), а его контроллер
   регистрируется последним — после всех слияний.

**Прежние подозрения сняты измерением:** EAA — владельцем в игре («нет, баг с перками на месте»
при выключенном EAA); он пишет в ванильную `DT_AttributeSystem` ровно одну строку, номер 7.

**Побочная находка, важная для пункта 6 (русификация):** MrS заодно ВЫКИДЫВАЕТ русский перевод
перков. Ванилька показывает «Тяжелая рука», он — «Heavy Blows».

**Гипотеза агента, которая НЕ подтвердилась** (записано, чтобы не повторяли): предполагалось, что
заглушки стоят выше сотого уровня либо вне сетки автора с шагом пять. Нет — они вперемешку с
настоящими перками на низких уровнях (между «15 Combo Master» и «25 Berserker») и во всех шести
ветках, а не только в Авторитете.

**Отдано владельцу на решение 10.09** — вопрос сведён к одному: какой потолок уровня он хочет.
Замены не трогают перки вовсе, все три взаимоисключающи с MrS (правят те же три таблицы):

| вместо MrS | что даёт | одобрений / загрузок |
|---|---|---|
| **Why Stop At 60 — Level 120** (Nexus 7) | потолок 120, своя кривая, больше очков | **132 / 6.2k — самый одобряемый в каталоге** |
| **MrSerji's Level 1000** (Nexus 15) | тот же автор, потолок 1000, БЕЗ переработки перков | 34 / 1.3k |
| **Why Are You Weak** (Nexus 6) | вдвое больше очков атрибутов | 65 / 2.3k |

Рекомендация агента — **Why Stop At 60**: тысяча уровней это марафон (сам пак отметил, что
скорость прокачки тем модом не решается), а владельцу скоро начинать долгую партию, и там
надёжность важнее размаха. Проверку берёт на себя агент: он умеет сам войти в игру, открыть
«Хар-ки» и снять экран.

### ✅ РЕШЕНО ВЛАДЕЛЬЦЕМ 2026-09-10 ~12:10 — и рекомендация агента ОТКЛОНЕНА

**Слово владельца:** «я хочу БЕЗ потолка уровня. Поэтому 1000 выглядит ближе к этому заказу.
И чтоб уровни качались быстро и часто. Кривая должна быть пологая, или линейная вообще».

Он выбрал НЕ то, что предлагал агент, и это важно записать: агент рассуждал про надёжность и
марафон, а у владельца был критерий, которого агент не знал, — **отсутствие потолка как таковое**.
Правильный ход был не советовать «120 надёжнее», а спросить, ЧТО он ценит в потолке.

**Сделано:** №29 выключен, взят **`MrSerji's Level 1000` (Nexus 15)** того же автора — скачан самим
движком (`kumm get 15 --root <пак>`), раскатан, `-Verify` чистый 63/63. Прогрессия та же (1000,
600 очков атрибутов, 121 099 очков умений), переработки перков нет.

**Проверено в игре агентом:** заглушек ноль, иконки настоящие, названия перков снова русские.

**Потеряно ТОЧНО:** только дополнительные ступени `Robust 2-5` и `Energetic 2-5`. Сами перки
ванильные и на месте — Живучесть → «Полнокровность» (+100 к здоровью = Robust), Выдержка →
«Выносливость» (запас сил на 25% быстрее = Energetic). Первая формулировка агента «веток нет»
была неточной и исправлена по снимкам.

### ⚠️ Пологую кривую купить нельзя — и это надо знать заранее

Кривая живёт в `DT_ExperienceSystemLevel` внутри пака, пак сжат Oodle, своей не написать без
Dev Kit (270 ГБ). **→ 2026-09-12: Dev Kit есть и кривая прочитана — 60 уровней, порог 12-го
уровня 61 825 сходится с замером ниже; CSV в `researches/conan-devkit/`, свой мод кривой —
фаза 1 эпика `plans/06`, форма кривой — развилка владельца.** Замер по трём состояниям на одном персонаже (опыт до следующего уровня):
ванилька в начале самая пологая (61 825), №29 и №15 в этом диапазоне идут близко.

**Настоящая ручка скорости — множители опыта, и они были НЕ ТРОНУТЫ.** В `ServerSettings.ini`
игры стоял общий 5.0, а под-множители по единице. Поднято 10.09:
`PlayerXPRateMultiplier` 5.0 → **12.0**, `Kill/Harvest/Craft` 1 → **2.0**. Копия до правки —
`ServerSettings.ini.before-xp-20260910`. **Число подбирается на ощупь — ждёт слова владельца.**

### 🔴 ДОЛГ, названный вслух: пак НЕ ведёт `ServerSettings.ini`

Правка множителей опыта живёт ТОЛЬКО в папке игры и умрёт при переустановке — это ровно то, чего
KUMM существует не допускать. Файл надо взять в манифест (как `Engine.ini` и `Game.ini`), а
значения — в `_config/`. Отдельная задача, до неё правки опыта считать временными.

---

## 🟡 6. Русификация модов

**Слова владельца:** «по ходу как моды ставишь, проверяй, чтобы у них была руссификация. Если нет
— будем сами руками пробовать переводить потом».

**Что известно (измерено прибором `_config/check-mod-localization.py` по всем 44):**
с русским переводом — **0**, с любым другим — **0**, **без механизма локализации вообще — 44**.
У Конана нет `.locres`; тексты живут в StringTable и таблицах данных внутри паков.

**Значит перевод = правка ассетов ВНУТРИ пака**, для каждого мода отдельно, и заново после каждого
обновления мода. **Оценка владельцу так и не дана** — это долг.

**НАХОДКА 2026-09-10, меняющая приоритет:** дело не только в том, что у модов НЕТ русского —
как минимум один мод **ОТБИРАЕТ уже имеющийся русский у самой игры**. `MrS_LevelAndAttributeOverhaul`
подменяет ванильное окно атрибутов, и вместе с ним перки теряют перевод: там, где игра пишет
«Тяжелая рука», «Сложные связки», «Берсерк», мод пишет «Heavy Blows», «Second Skin», «Berserker».
Снято контрольным прогоном со снимками экрана (пункт 5).

Это дешевле общей задачи перевода и полезнее: **сперва найти тех, кто ломает существующий перевод,
и решить по каждому** — они видны по монтированию в ванильные пути, а не в свою папку
(`grep -v '/Mods/' _config/backup/mods-asset-catalog.txt`), — и только потом браться за перевод
того, у чего перевода не было изначально.

**Следующий шаг:** оформить честную оценку работы (что потребуется, сколько это в модах и в
инструментах) и отдать владельцу на решение. Кода не писать до его слова.

> ⚠️ **ПОПРАВКА 2026-09-12 по Dev Kit:** фраза выше «у Конана нет `.locres`» — неверна. В
> исходниках кита лежит полный конвейер локализации игры: 18 целей, манифесты и `.locres` на
> 13 языков, включая `ru` (`UE4/Content/Localization/`). Верно другое: у МОДОВ своих `.locres`
> нет, а их тексты — `FText` с ключами (в `UIMod_Hosav` 369 английских фраз в 46 виджетах).
> Оба пути перевода описаны в `researches/conan-devkit/README.md` §7.1; работа — фаза 2
> эпика `plans/06`.

---

### 📏 Замер 12.09.2026 — сколько текста везёт каждый мод

Вопрос владельца при закрытии чата: «есть моды, которые много текста тянут?». Померено, а не
прикинуто. Прибор — `UnrealPak -List`/`-Extract` кита плюс починенный `ftext_extract.py`
(он теперь видит ОБЕ формы `FText`, включая литералы в байткоде Blueprint).

**Точный счёт по мелким модам** (распаковка 15 модов — 3 секунды, 69 МБ):

| мод | ключей `FText` | уникальных фраз |
|---|---|---|
| No_Building_Placement_Restrictions | 348 | 189 |
| IdeaPoet_EditAppearance | 199 | 171 |
| DamageNumber | 140 | 107 |
| Simple_Minimap | 125 | 100 |
| SortingBox | 66 | 46 |
| PackRat_Helper | 47 | 30 |
| ModControlPanel | 25 | 20 |
| LLHighlight | 20 | 16 |
| ProximityParty | 18 | 14 |
| ChestLabels | 2 | 2 |
| CorpseStripper · MyAUThrallArmy · StorageReachExpanded · Thrall_Scanner · storage_size | **0** | — |
| *(для сравнения)* UIMod_Hosav | **939** | 359 |

**Прокси по крупным модам** (только листинг контейнеров, 11 секунд на все 45 — распаковка их
дорога, 1.5 ГБ у одного): интерфейсных ассетов больше всего у `Beyond_Decor_II` (470),
`EnhancedArmoryAndArsenal` (272), `HighmanesArsenal` (206), `Emberlight` (105),
`Asghaard_Ancient_Civilization` (69). Это НЕ счёт текста, а его верхняя граница по «сколько
виджетов»; настоящий счёт по ним — отдельный заход с распаковкой.

**Главный вывод замера — объём НЕ равен приоритету.** `MrS_LevelAndAttributeOverhaul` по этой
таблице почти пуст, а вредит больше всех: он подменяет ванильное окно атрибутов и **отбирает у
перков русский, который в игре УЖЕ ЕСТЬ** («Heavy Blows» вместо «Тяжелая рука»). Терять готовый
перевод хуже, чем не иметь своего.

**Порядок работ, вытекающий из замера:**
1. `MrS_LevelAndAttributeOverhaul` — вернуть отобранное. Самая заметная боль на каждой прокачке.
2. `No_Building_Placement_Restrictions`, `IdeaPoet_EditAppearance`, `Simple_Minimap`,
   `DamageNumber` — 100–190 фраз каждый, дёшево тем же приёмом `.locres`, что и Hosav.
3. Крупные (`EnhancedArmoryAndArsenal`, `Emberlight`, арсеналы) — сперва распаковать и посчитать,
   потом решать: там могут быть сотни имён предметов, а это уже объём для отдельного интервью.
4. Пять модов текста не несут вовсе — их можно вычеркнуть из списка навсегда.

## 🟢 7. Комбат-лог мешает — ответ дан, в игре не проверен

**Слова владельца:** «мод про комбат лог мне мешает. Цифры над героями — нравятся. А окошки вверху
экрана — не нравятся».

**Ответ со страницы автора (`The Damage Meter`, Xevyr):** зажать **F1** → минус в левом верхнем
углу прячет главное окно; **у боевого журнала своя кнопка** на главном окне, прячется отдельно;
всплывающие цифры отключаются отдельным пунктом настроек, то есть остаются.

**Осталось:** владелец не подтвердил, что помогло.

---

## 🟢 8. Конфликт трёх модов Xevyr по клавише F1 — ПОДТВЕРЖДЁН журналом

Раньше это была догадка по описаниям. Теперь измерено: строка `DT_UIModuleTable / XCS_Panel`
переписывается **четыре раза** — это панель настроек Xevyr, за неё дерутся Simple Minimap,
Proximity Party и The Damage Meter.

**Общая картина конфликтов сборки (замер 09.09):** 3361 запись в общие таблицы, 3313 уникальных
строк, **спорных (переписаны 2+ раз) — 46**, то есть 1.4 %. Кроме `XCS_Panel` это добыча с
оборотней (`LootTable_Resource`) и точки появления NPC (`SpawnDataTable`), по 2 раза каждая.
Побеждает тот, кто грузится последним, а порядок задаём мы в `modpack.json`.

**Чего НЕ знаем:** журнал не называет, КАКОЙ мод пишет каждую строку. Чтобы назвать пары
поимённо, надо сопоставлять содержимое паков — отдельная посильная работа.

---

## ✅ Закрыто в этот вечер

- **FOV 75** — по просьбе владельца, закреплено в `_config/Game.ini`.
- **Подсказки управления** — владелец нашёл выключатель в настройках интерфейса, ключ
  `showContextualControls=False` пойман сверкой и закреплён в паке, чтобы раскатка не вернула.
- **Зонд снаряда убивал весь Lua-слой UE4SS** — найден перебором, выключен в манифесте
  (`bugs/07`). Причина ВНУТРИ зонда не найдена, включать нельзя.
- **Дрожь камеры на средних дистанциях** — вылечена причиной, принята владельцем.
- **Подозрения на EAA и Immersive Warriors** — сняты ИЗМЕРЕНИЕМ, а не рассуждением.
- **Устройство дождя** — снято офлайн, цель названа.
- **Харнесс вождения игры** — построен и проверен в бою.

## Долги из плана 03, всё ещё открытые

1. **Immersive Warriors против Glory of Combat** — владелец спрашивал, что ближе к ванили и лору.
   Материал собран (`docs/08`, записи 7 и 97), **ответ не отдан**.
2. **`dc SRE.set 20000`** — владелец просил радиус хранилищ 20000, мод ставит 15000. Команда
   вводится в игре и сохраняется между перезапусками, проверка — `dc SRE.show`. **Не сделано.**
3. **Баг 06** — раскатка с `-Only` молча обрезает `modlist.txt`. Пока не починен — **Конана
   раскатывать только целиком**.
4. **Графика** — замер базы `perf-harness` в Конане не делали ни разу; `DLSSQuality` 4 вместо 5;
   пересмотр `r.Fog=0`.
5. **Идея 02, шаг 1** — опыт со спавном (`SpawnActor` из Lua). Теперь НЕ требует владельца:
   харнесс запускает игру сам.
